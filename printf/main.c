#include <stdio.h>
#include "printf.h"

/*int sum_positive(int count, ...)
{
    va_list args;
    va_start (args, count);

    int i = 0;
    int min = -1;
    int num;
    while (i < count)
    {
        num = va_arg(args, int);
        if (num > 0)
        {
            if (min == -1 || min > num)
                min = num;
        }
        i++;
    }
    va_end (args);
    return (min);
}*/

int main(void)
{
    ft_printf("%");
}